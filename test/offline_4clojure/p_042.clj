;; Factorial Fun - Easy
;; Write a function which calculates factorials.
;; tags - math
;; restricted - 
(ns offline-4clojure.p42
  (:use clojure.test)
  (:use offline-4clojure.core))

(??
  (do
    (deftest test4clj-0 (is (= (__ 1) 1)))
    (deftest test4clj-1 (is (= (__ 3) 6)))
    (deftest test4clj-2 (is (= (__ 5) 120)))
    (deftest test4clj-3 (is (= (__ 8) 40320))))
  __)