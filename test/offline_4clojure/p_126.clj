;; Through the Looking Class - Easy
;; Enter a value which satisfies the following:
;; tags - fun:brain-teaser
;; restricted - 
(ns offline-4clojure.p126
  (:use clojure.test)
  (:use offline-4clojure.core))

(??
  (do (deftest test4clj-0 (is (let [x __] (and (= (class x) x) x)))))
  __)